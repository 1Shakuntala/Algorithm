1.     	String n = "1234";
    	  char[] digits = n.toCharArray();  // ['1', '2', '3', '4'] perform operation accordingly

2.      // Convert int to String, then to char array
        String str = Integer.toString(n);
        char[] digits = str.toCharArray();
        int len = digits.length;

3.      Array
	      int marks[] = new int[5];  //
	      System.out.println(Arrays.toString(marks));
